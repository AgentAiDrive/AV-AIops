import express from 'express';import cors from 'cors';import morgan from 'morgan';import rateLimit from 'express-rate-limit';import dotenv from 'dotenv';dotenv.config();const app=express();app.use(cors());app.use(express.json({limit:'1mb'}));app.use(morgan('dev'));app.use(rateLimit({windowMs:60000,max:120}));const PORT=process.env.PORT_SLACK||0;app.get('/healthz',(req,res)=>res.json({ok:true}));app.get('/readyz',(req,res)=>res.json({ok:true}));app.get('/api/test',(req,res)=>res.json({ok:true,service:'mcp-slack'}));app.post('/api/postMessage',(req,res)=>{const {channel='#av-ops',text='Hello from mock Slack MCP'}=req.body||{};const url=`https://mock.slack.local/${encodeURIComponent(channel)}/${Date.now()}`;return res.json({ok:true,channel,ts:Date.now().toString(),url});});app.listen(PORT,()=>console.log('mcp-slack listening',PORT));