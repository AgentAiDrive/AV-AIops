import express from 'express';import cors from 'cors';import morgan from 'morgan';import rateLimit from 'express-rate-limit';import dotenv from 'dotenv';dotenv.config();const app=express();app.use(cors());app.use(express.json({limit:'1mb'}));app.use(morgan('dev'));app.use(rateLimit({windowMs:60000,max:120}));const PORT=process.env.PORT_ZOOM||0;app.get('/healthz',(req,res)=>res.json({ok:true}));app.get('/readyz',(req,res)=>res.json({ok:true}));app.get('/api/test',(req,res)=>res.json({ok:true,service:'mcp-zoom'}));app.post('/api/webinar/create',(req,res)=>{const {topic='Demo Webinar',when=new Date().toISOString()}=req.body||{};const id=Math.floor(Math.random()*1e8).toString();return res.json({ok:true,id,join_url:`https://zoom.mock/j/${id}`,topic,when});});app.listen(PORT,()=>console.log('mcp-zoom listening',PORT));