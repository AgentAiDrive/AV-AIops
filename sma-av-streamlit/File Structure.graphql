sma-av-streamlit/
  core/
    agents/
      fixed/
        __init__.py
        registry.py               # declares fixed agents & their capabilities
        policies.py               # risk classes, windows, RBAC roles
    recipes/
      schema.py                   # dataclasses / Pydantic models
      sop_compiler.py             # NEW: SOP → orchestrator.yaml + fixed/recipes/*.yaml
      storage.py                  # path helpers, save/load
    orchestrator/
      runner.py                   # consumes orchestrator.yaml, loads bound fixed recipes
    ui/
      page_tips.py
  data/
    recipes/
      orchestrator/               # orchestrator recipes live here
      fixed/                      # per fixed agent recipe outputs live here
  pages/
    3_Chat.py                     # (or your page name) — updated to call compiler on /sop
